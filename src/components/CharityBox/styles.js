import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles(theme => ({
    fontSpacing: {
        letterSpacing: `2px`,
        textAlign: `center`
    }
    
}));

export { useStyles };