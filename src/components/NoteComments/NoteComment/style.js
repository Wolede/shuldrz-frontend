import { makeStyles } from '@material-ui/core/styles'

const useStyles = makeStyles(theme => ({
    root: {
        
    },
    comment: {
        overflowWrap: 'anywhere'
    }
}))

export { useStyles }