import { makeStyles } from '@material-ui/core/styles'

const useStyles = makeStyles(theme => ({
    root: {
        // overflowY: 'scroll',
        // height: '19.75rem'
    },
}))

export { useStyles }